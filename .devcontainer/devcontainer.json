{
    "name": "Python 3 - HackRx",
    "image": "mcr.microsoft.com/devcontainers/python:3.12",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libgl1-mesa-glx && sudo playwright install-deps && playwright install"
  }